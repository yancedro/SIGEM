import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
// Se tiver import do BrowserRouter aqui, pode apagar!

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)